export interface Professional {
  id: number;
  name: string;
  price?: string;
  image?: string;
  verified?: boolean;
  category?: string;
  topExpertise?: boolean;
  description?: string;
  linkedin?: string;
}
